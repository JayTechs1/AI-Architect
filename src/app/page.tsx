import SpotlightHero from "@/components/SpotlightHero";

// Landing page: full-screen cursor-spotlight hero (per spec). The previous
// marketing sections live on in /about, /pricing, /tools, /contact,
// /developers — and in git history if we want them back on the home page.
export default function Home() {
  return <SpotlightHero />;
}
